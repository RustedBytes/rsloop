[project]
name = "rsloop-rust-example"
version = "0.1.0"
requires-python = ">=3.10"

[build-system]
requires = ["maturin>=1.9.4,<2"]
build-backend = "maturin"

[tool.maturin]
module-name = "rsloop_rust_example"
