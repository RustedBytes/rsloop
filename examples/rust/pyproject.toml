[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "rsloop-rust-example"
version = "0.1.0"
requires-python = ">=3.8"

[tool.maturin]
module-name = "rsloop_rust_example"
